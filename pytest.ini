[pytest]
testpaths = tests
pythonpath = src
addopts = -q --strict-markers
markers =
    slow: uzun suren test (performans, toplu isleme)
    gui: Qt widget gerektiren test
    integration: uctan uca akis testi
filterwarnings =
    error::DeprecationWarning:luma_atelier.*
